features:
  - sentry-integration-tests

test_filter: "::sentry::"

runner:
  env:
    SENTRY_DSN:

matrix:
  version: [latest]

# changes to these files/paths will invoke the integration test in CI
# expressions are evaluated using https://github.com/micromatch/picomatch
paths:
  - "src/sinks/sentry.rs"
  - "src/sinks/util/**"
  - "scripts/integration/sentry/**"
